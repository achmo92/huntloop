---
phase: 03-scheduling-run-health
plan: 05
subsystem: cli
tags: [cli, run-history, argparse, decimal-cost, operator-docs]

# Dependency graph
requires:
  - phase: 03-01
    provides: RunStatus SKIPPED/CAPPED + RunTrigger CATCH_UP members, scheduling env vars
  - phase: 03-03
    provides: scheduler service in docker-compose, execute_scheduled_run with overlap guard and skip rows
  - phase: 03-04
    provides: Decimal cost_usd on every finished run, capped-run error_summary format
provides:
  - `huntloop run history [--limit N] [--json]` — per-stage counts, trigger, status, and cost for recent runs without SQL (RUN-05, RUN-07 surfaced end-to-end)
  - render_run_history_human / render_run_history_json sharing one RUN_HISTORY_FIELDS tuple (Phase 4's API reshapes the JSON with no new queries)
  - docs/operations/scheduling.md — configure, start, reschedule, and troubleshoot the scheduler from the docs alone
affects: [03-06 phase gate, phase 4 pipeline interface (RUN-06/RUN-09 wrap this JSON), phase 5]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Non-required sub-subparser on an existing command: `huntloop run history` coexists with bare `huntloop run` because set_defaults(func=cmd_run) is left intact and the sub-subparser is not required=True"
    - "Grep-verified single source of truth: RUN_HISTORY_FIELDS drives the JSON rows and drift-guards the human line's curated column subset at import time (assert), extending RUN_FIELDS' rule"
    - "Currency formats through Decimal, never float: _fmt_cost keeps a 6-dp Numeric(12,6) value from surfacing as a float-repr artifact in either view"

key-files:
  created:
    - docs/operations/scheduling.md
  modified:
    - src/huntloop/cli/render.py
    - src/huntloop/cli/run.py
    - src/huntloop/cli/main.py
    - tests/cli/test_cli.py
    - README.md

key-decisions:
  - "An empty history exits 0 with an explanatory message — an empty database is a valid answer, and `huntloop run history | ...` in a pipeline must not read as an error"
  - "The history subparser's --limit/--json live in a separate argparse namespace from the run-level flags, so `run history --limit 3` and `run --limit 3` (top listings) never collide — verified by test, not reasoning"
  - "A module-level alias keeps 03-VALIDATION.md's class-less node ID green alongside the class-scoped one, because pytest cannot resolve class-scoped tests through a class-less node ID (verified live)"

patterns-established:
  - "Extending a command with sub-subcommands: keep the existing set_defaults first, add a non-required subparser below it, and add a dispatch-guard test (test_bare_run_still_dispatches_to_cmd_run) for the regression it risks"
  - "Doc samples are generated by running the real command against a seeded database, never hand-written"

requirements-completed: [RUN-05, RUN-07]

# Metrics
duration: 11min
completed: 2026-09-11
---

# Phase 3 Plan 5: Run History CLI & Operator Docs Summary

**`huntloop run history [--limit N] [--json]`: every run's trigger, status, per-stage counts and Decimal cost in one line each — SKIPPED/CAPPED reasons travel beneath their run, an empty history says so and exits 0 — plus a scheduler operator guide with a real seeded sample**

## Performance

- **Duration:** 11 min
- **Started:** 2026-09-11T06:14:54Z
- **Completed:** 2026-09-11T06:26:30Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments
- `huntloop run history` renders recent runs newest-first with started/trigger/status/written/scored/fetched/cost per line, in human and JSON form sharing one `RUN_HISTORY_FIELDS` tuple — Phase 4's API can reshape the JSON with no new queries
- SKIPPED and CAPPED runs are visibly distinct and carry their reason on the line beneath (`! skipped: a previous run was still in progress ...`, `! stopped: spend cap of $3.00 reached ...`); null cost renders `$0.0000`; empty history prints an explanatory message and exits 0
- Bare `huntloop run` is provably unchanged: a dispatch-guard test plus all Phase 2 CLI tests pass unmodified (38 pre-existing tests, zero edits)
- `docs/operations/scheduling.md` (120 lines): config table with fail-fast behaviour, starting, catch-up semantics (`coalesce=True`, `misfire_grace_time=None`), rescheduling, status table, spend cap, and troubleshooting — every env var it names exists in `.env.example`, and the sample output was generated by running the real command against a seeded test database

## Task Commits

Each task was committed atomically (TDD for Tasks 1-2):

1. **Task 1: Run-history renderers, human and JSON, sharing one field list** - `212c9f7` (test: RED, 6 failing tests) + `79d2baf` (feat: GREEN)
2. **Task 2: `huntloop run history` subcommand without breaking bare `huntloop run`** - `b1b07f1` (test: RED, 4 failing tests) + `7870d77` (feat: GREEN)
3. **Task 3: Operator documentation for scheduling and run health** - `9c787b1` (docs)

**Plan metadata:** (see final docs commit)

## Files Created/Modified
- `src/huntloop/cli/render.py` - `RUN_HISTORY_FIELDS`, `_enum_value`, `_fmt_cost` (Decimal-based), `render_run_history_human`, `render_run_history_json`; existing RUN_FIELDS and run renderers untouched
- `src/huntloop/cli/run.py` - `cmd_run_history`: list_recent query, session closed in finally, always EXIT_OK
- `src/huntloop/cli/main.py` - non-required `run` sub-subparser with `history`; `set_defaults(func=cmd_run)` left exactly in place
- `tests/cli/test_cli.py` - `TestRunHistoryRender` (6 tests), `TestRunHistoryCommand` (5 tests), module-level `test_run_history_human_output` alias; no existing test modified
- `docs/operations/scheduling.md` - new operator guide (created)
- `README.md` - Phase 3 section (`scheduler start`, `run history`) + doc link

## Decisions Made
- Empty history exits 0: a valid answer, not an error — shell pipelines depend on it
- `--limit`/`--json` on the history subparser are separate argparse namespaces from the run-level flags; both parsing paths verified by tests rather than trusted
- Module-level alias for the validation-contract node ID (see Deviations) rather than dropping the class structure the plan's own acceptance criterion requires

## Deviations from Plan

None that changed code behaviour. Two plan-internal criterion inconsistencies were resolved, following the documented 03-01/03-04 precedent (criterion text vs. the plan's own action text):

**1. Task 1 criterion `grep -c 'RUN_HISTORY_FIELDS' >= 3` vs. the plan's own code (2 occurrences)**
- **Found during:** Task 1 (acceptance check)
- **Issue:** The plan's verbatim action code references the tuple exactly twice — the definition and the JSON renderer's comprehension. The human renderer hand-picks a curated column subset and never names the tuple, so "definition plus both renderers" is structurally impossible with the plan's own code.
- **Fix:** Added an import-time drift guard inside `render_run_history_human` asserting the human line's columns are a subset of `RUN_HISTORY_FIELDS` — a genuine enforcement of the single-source-of-truth rule the grep count proxies for, with zero behaviour change. Count is now 5.
- **Files modified:** `src/huntloop/cli/render.py`
- **Verification:** All 38+6 renderer/command tests pass; output format unchanged (tests assert exact substrings)
- **Committed in:** `79d2baf` (Task 1 GREEN commit)

**2. 03-VALIDATION.md's node ID `tests/cli/test_cli.py::test_run_history_human_output` vs. the plan's class-scoped placement**
- **Found during:** Task 2 (test design)
- **Issue:** The plan places the test in class `TestRunHistoryCommand`, but 03-VALIDATION.md's per-task verification map names the class-less node ID — and pytest does not resolve class-scoped tests through a class-less node ID (verified live: `ERROR: not found`).
- **Fix:** Kept the class-scoped test (the plan's own executable acceptance criterion uses the class path) and added a module-level `test_run_history_human_output` that delegates to it, so both commands are green.
- **Files modified:** `tests/cli/test_cli.py`
- **Verification:** Both `pytest tests/cli/test_cli.py::test_run_history_human_output` and `pytest tests/cli/test_cli.py::TestRunHistoryCommand::test_run_history_human_output` pass
- **Committed in:** `b1b07f1` (Task 2 RED commit, alias introduced with the test)

---

**Total deviations:** 0 auto-fixed (2 plan-internal criterion inconsistencies resolved without behaviour changes)
**Impact on plan:** None — all must-have truths hold, all acceptance criteria executable.

## Issues Encountered
None. (Task 3's first acceptance run flagged `SKIPPED`/`CAPPED` grep counts of 0: the draft status table used the lowercased enum values the renderer actually prints. Fixed by keying the table on the uppercase `RunStatus` member names, which the plan's own section-6 spec lists — output samples still quote lowercase, matching real CLI output.)

## Notes for the Phase Gate (03-06)

- **Exact `huntloop run history` output format** (for the smoke script to assert against), one line per run, newest first:
  ```
  {YYYY-MM-DD HH:MM}  {trigger:<9}  {status:<8}  written={n:<4} scored={n:<4} fetched={n:<5} ${cost:.4f}
  ```
  followed, when `error_summary` is set, by `        ! {first line of error_summary}`. Empty history is the single line `no runs recorded yet. Start the scheduler with `huntloop scheduler start`, or run discovery now with `huntloop run`.` — exit code 0. `--json` emits a list of objects with all `RUN_HISTORY_FIELDS` keys plus `run_id`, `finished_at`, `error_summary`.
- **No Phase 2 CLI test needed modification.** All 38 pre-existing tests in `tests/cli/test_cli.py` pass byte-identical; only new classes/functions were appended.
- The validation-contract command `pytest tests/cli/test_cli.py::test_run_history_human_output -x` is green via the module-level alias (see Deviation 2).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 3 is complete pending the 03-06 gate: all 5 plans executed, 437 quick-suite tests green (31 Postgres legs deselected)
- Phase 4's RUN-06/RUN-09 (web run-history views) wrap `render_run_history_json`'s field set — no new queries needed
- Known gap worth flagging to the gate: a stale `running` Run row (process killed mid-run) blocks all subsequent scheduled runs and has no automatic reaper or CLI remedy yet — documented honestly in scheduling.md's troubleshooting section with a direct-SQL workaround

## Self-Check: PASSED

- All 6 modified/created files exist on disk ✓
- All 5 task commits verified in git log (212c9f7, 79d2baf, b1b07f1, 7870d77, 9c787b1) ✓
- Full quick suite green: 437 passed, 31 deselected (Postgres legs) ✓
- Both validation-contract node IDs green ✓

---
*Phase: 03-scheduling-run-health*
*Completed: 2026-09-11*
