"""LOOP-07 apply path re-validates through CriteriaPayload. Owned by plan 05-04."""

import pytest

pytest.importorskip("huntloop.loop.apply")


def test_apply_validates_through_criteria_payload():
    pytest.fail("not yet implemented — owned by plan 05-04")
