"""D-06 generation and D-10 dedup. Owned by plan 05-05."""

import pytest

pytest.importorskip("huntloop.loop.generate")


def test_generate_is_idempotent_for_pending_signature():
    pytest.fail("not yet implemented — owned by plan 05-05")
