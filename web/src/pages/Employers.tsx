import { EmptyState } from "@/components/EmptyState"

export default function Employers() {
  return (
    <EmptyState
      title="Employers"
      description="The employers we watch — and how well we can cover them — will appear here"
      actionLabel="Add employers"
    />
  )
}