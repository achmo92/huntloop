import { EmptyState } from "@/components/EmptyState"

export default function Criteria() {
  return (
    <EmptyState
      title="Criteria"
      description="Your search criteria — what you're looking for — will live here"
      actionLabel="Set up your search"
    />
  )
}