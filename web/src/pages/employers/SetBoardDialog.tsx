import type { CompanyOut } from "@/pages/criteria/types"

interface SetBoardDialogProps {
  company: CompanyOut | null
  onClose: () => void
}

// RED stub (GAP-14): the real dialog lands in the GREEN commit.
export function SetBoardDialog(_props: SetBoardDialogProps) {
  return null
}
