import { EmptyState } from "@/components/EmptyState"

export default function Runs() {
  return (
    <EmptyState
      title="Runs"
      description="Each discovery run and its outcome will appear here"
      actionLabel="Run discovery"
    />
  )
}