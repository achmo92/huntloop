import { EmptyState } from "@/components/EmptyState"

export default function Settings() {
  return (
    <EmptyState
      title="Settings"
      description="Model, schedule, and spend settings will live here"
    />
  )
}